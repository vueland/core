# Flex utilities

A set of Vueland UI utility classes for `flex` tasks.

## Example

```html
<div class="flex">Content</div>
```

## Purpose

These classes help solve common interface tasks quickly without writing additional CSS. Use them for small local adjustments to layout, sizing, text and visual states.
